# This file marks the directory as a Python package.
# It must be present in every sub-folder under app/ so that
# Python's import system can resolve modules like:
#   from app.config.settings import settings
