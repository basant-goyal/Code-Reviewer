import HomePage from './pages/HomePage'

/*
 * App.jsx — Root Application Component
 * ========================================
 * WHY THIS FILE EXISTS:
 *   This is the top-level component that Vite renders into #root.
 *   Keeps the root simple — just mounts the HomePage.
 *   If routing is added later (React Router), it goes here.
 */

export default function App() {
  return <HomePage />
}
